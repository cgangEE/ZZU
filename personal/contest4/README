A. Destorying the bus stations

B. A simple stone game

C. Ugly Windows
给定一个n*m的(1<=n,m<=100)的矩形，矩形上最多有A-Z个被这些矩形标记的窗口，一些窗口被覆盖，求最上面的窗口的编号。
模拟 对于矩形上已经有的标记，用他们来更新这些标号x、y轴的最小值。对于一个标记便可以求得这个矩形的左上角和右下角的坐标，便可枚举这个窗口的边框，判断是否都为该标记，还要枚举边框的内侧是否都为空白。

D. Tornado

E. Minimal Ratio Tree
给定一个n（1<=n<=15)个点的完全图，求一个顶点数为m的树，使得他们的边权之和/点权之和最小。
最小生成树 枚举m个点，求最小生成树，在求出其中边权之和/点权之和最小的。


F. Parade

G. Priest John's Busiest Day

H. Ping pong
给定一个长为N(3<=N<=20000)的序列，求有多少个三元组，满足升序或者降序。
树状数组 用树状数组从前向后、从后向前求得在i之前比a[i]小的数字的个数f[i]，和在i之和比a[i]小的数字的个数g[i]，那么以i为第二个数的升序的三元组的个数就为前面比他小个个数*后面比他大的个数，同理可求得降序三元组的个数。

I. Timer

J. Elevator
