武汉大学e鸣杯

A. Which is Greater 
给定两个数字，判断第一个数字是否大于第二个数字。
水题

B. Rectangle Area
给定一个矩形的平行x、y轴的对角线上的两个坐标，求矩形面积。
水题

C. Count the Vowels
给定一个字符串s，求其中元音字母的数量。
水题

D. Jewels from Heaven
在一个10000*10000的大矩形，给定三个点A、B、C,求在这个矩形离A点最近区域的面积。
半平面交 枚举B、C两点x，x与A做垂直平分线l，l上的点到A和x距离是相等的，而l把平面平分为两半，靠近A那一半平面上的点距离A点近。与B、C的垂直平分线得到的半平面，做半平面交。

E. Rational and Irrational
给定P、N<=1000，求最小的A/B,A、B<=N，满足A/B不小于Sqrt(P)。
二分查找 枚举A，二分查找到最小的B，使A/B不小于sqrt(P)。

F. Rational and Irrational Plus Plus
给定P、N<=10^9，求最小的A/B,A、B<=N，满足A/B不小于Sqrt(P)。
法雷数列+二分查找 n阶的法雷数列的构造，开始是左侧是1/1，右侧是1/0，而后中间的分数是1+1/1+0=2/1，而1/1<2/1<1/0，这样一直生成中间的分数，直到它的分子大有n，就停止构造。而二分查找就是不断比较中间生成的分数与sqrt(p)的大小关系，从而缩小解的范围。

G. Sea Sky

